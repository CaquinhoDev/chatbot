module.exports = {
  skipGroups: false, // Bot skip groups?
  skipPrivate: false, // Bot skip private chats?
  devMode: false, // Is the bot in dev mode?
  useQrCode: true, // if true, the qr code will be printed on the terminal, if false, the pairing code will be used. recommended: true
  whitelist: ["120363239769112321@g.us", "5511913372146@s.whatsapp.net"], // JIDs that the bot should respond to (if dev mode enabled)
};
